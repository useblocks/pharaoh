# typescript-fully-covered

TypeScript classifier-path fixture. `SessionManager` declares two methods (`open`, `ready`) → method-rich-class rule → `behavioral`. Four needs cite the file via `:source_doc:`. Two `throw new <Class>(...)` sites expose `SessionError` and `InvalidUserError`; both names appear in CREQ content. Expected verdict: `overall: "pass"`.
