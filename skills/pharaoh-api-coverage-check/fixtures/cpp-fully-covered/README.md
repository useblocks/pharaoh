# cpp-fully-covered

C++ classifier-path fixture. Two free functions raise via `throw` → exception-surface rule → `behavioral`. Four needs cite the file via `:source_doc:`. Two throw sites expose `CatalogError` and `InvalidPathError`; both names appear in CREQ content. Expected verdict: `overall: "pass"`.
