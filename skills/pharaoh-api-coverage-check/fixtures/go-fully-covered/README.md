# go-fully-covered

Go classifier-path fixture. `SaveCatalog` has three top-level statements in its body → non-trivial-function-body rule → `behavioral`. Four needs cite the file via `:source_doc:`. Go has no raise-site regex (errors travel via `error` return values), so the raise-site axis is vacuously satisfied with `total: 0`. Expected verdict: `overall: "pass"`.
