# python-uncovered-raises

Behavioral file (raises four distinct exception classes) cited by five needs. Only one raised class (`JamaAuthError`) appears in any CREQ's title or content; the other three (`JamaArtifactTypeError`, `JamaValueMapError`, `JamaSkippedValueError`) are absent from the catalogue. File coverage passes; raise-site coverage fails. Expected verdict: `overall: "fail"` with the three uncovered classes listed.
