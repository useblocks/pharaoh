# passing-case

Canonical happy path: one CREQ cites one exception class that is raised, names one function that is defined, declares no type-framework, no weasel words, no unbounded quantifier. All seven mechanical axes pass; the subjective branch-count axis scores 3 because the single shall-clause matches the function's single observable branch. `overall == "pass"` and `blockers` is empty.
