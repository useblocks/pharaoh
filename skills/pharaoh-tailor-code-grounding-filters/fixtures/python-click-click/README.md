# python-click-click

Click-based CLI — contrasts with Typer fixture by NOT emitting the `Opt`
morphology in the kebab filter. Validates that the `morphology_prefixes`
parameter is framework-specific, not Python-wide. Also validates the
"env-var style: none" branch when fewer than 3 uppercase-prefix identifiers
are present.
